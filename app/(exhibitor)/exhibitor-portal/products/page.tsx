"use client";

import Builder from "@/components/builder";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Builder />
    </div>
  );
};

export default page;
